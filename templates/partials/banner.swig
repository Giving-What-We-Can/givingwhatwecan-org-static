<div id="banner" {% if bannerImage %}{{style="background-image:url('{{bannerImage}}')"}}{% endif %}>
	<div class="header">
		<div class="container">
			<div class="row">
				<div class="col-sm-12">
					<h1 class="page-title">{{ title }}</h1></div>
			</div>
		</div>
	</div>
</div>